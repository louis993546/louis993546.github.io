{
  "name": "website",
  "version": "1.0.0",
  "description": "Louis Tsai's personal blog migrated from Ghost and built with Eleventy.",
  "license": "MIT",
  "author": "Louis Tsai",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "build": "npx @11ty/eleventy",
    "dev": "npx @11ty/eleventy --serve",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@11ty/eleventy": "^3.1.6"
  },
  "devDependencies": {
    "esbuild": "^0.28.1",
    "npm-run-all": "^4.1.5",
    "typescript": "^6.0.3"
  }
}
